% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/5.ModuleAnnotation.R
\name{functional_annotation}
\alias{functional_annotation}
\title{Annotate every module in a canonical module manifest}
\usage{
functional_annotation(
  subtype_file = NULL,
  base_input_path = "./ModuleSelection/",
  base_output_path = "./FunctionalAnnotation/",
  network_method = c("String", "physicalPPIN", "chengF"),
  module_method = c("Louvain", "WF"),
  module_manifest_file = NULL,
  gene_set_file = NULL,
  gene_sets = NULL,
  background_gene_file = NULL,
  background_genes = NULL,
  databases = NULL,
  species = "Homo sapiens",
  subtypes = NULL,
  subtype_column = "Subtype",
  pvalueCutoff = 0.05,
  pAdjustMethod = "BH",
  pAdjustCutoff = 0.05,
  qvalueCutoff = 0.2,
  minGSSize = 10,
  maxGSSize = 500,
  top_n = 15,
  strict = TRUE,
  write_module_files = TRUE
)
}
\arguments{
\item{subtype_file}{Optional phenotype file used only to filter subtypes.}

\item{base_input_path}{A \code{module_manifest.tsv} file or ModuleSelection root.}

\item{base_output_path}{New annotation output directory.}

\item{network_method}{Networks retained from the manifest.}

\item{module_method}{Module algorithms retained from the manifest.}

\item{module_manifest_file}{Optional explicit manifest path; takes priority
over \code{base_input_path}.}

\item{gene_set_file}{Optional \code{.tsv}, \code{.csv}, \code{.xlsx} or \code{.gmt} gene-set
file. Tabular files require \code{term_id} and \code{gene}; \code{database}, \code{category}
and \code{description} are optional.}

\item{gene_sets}{Optional in-memory gene-set data frame with the same
columns as \code{gene_set_file}.}

\item{background_gene_file}{Optional tabular file containing background
genes in \code{gene}, \code{gene_symbol}, \code{symbol} or the first column.}

\item{background_genes}{Optional character vector of background genes.}

\item{databases}{Optional database names to retain. When gene sets are not
supplied, defaults to \code{GO_BP}, \code{KEGG} and \code{HALLMARK}.}

\item{species}{Species passed to \code{msigdbr} when built-in sets are used.}

\item{subtypes}{Optional explicit subtype filter.}

\item{subtype_column}{Subtype column in \code{subtype_file}.}

\item{pvalueCutoff}{Raw p-value threshold.}

\item{pAdjustMethod}{Method passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}.}

\item{pAdjustCutoff}{Adjusted p-value threshold.}

\item{qvalueCutoff}{Q-value threshold; BH adjusted p-values are reported as
q-values in this over-representation implementation.}

\item{minGSSize, maxGSSize}{Inclusive gene-set size limits.}

\item{top_n}{Number of terms retained per module and database in the top
table.}

\item{strict}{Whether to verify module file checksums.}

\item{write_module_files}{Whether to write one result file per module.}
}
\value{
The unified annotation table with output file paths in attributes.
}
\description{
This chaining-compatible replacement for subnetDR's
\code{functional_annotation()} reads the exact modules emitted by step 4 instead
of rediscovering aggregate node files. It writes one unified table, a top-N
table, module-level files and QC. A custom gene-set table/GMT can be supplied
for reproducible or offline runs; otherwise GO biological process, KEGG and
Hallmark sets are loaded with \code{msigdbr}.
}
