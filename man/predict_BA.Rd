% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/8.BindingScore.R
\name{predict_BA}
\alias{predict_BA}
\title{Predict or import binding scores for selected modules only}
\usage{
predict_BA(
  selected_modules_file,
  seq_smiles_manifest_file,
  output_base,
  binding_score_file = NULL,
  predictor = NULL,
  score_direction = c("lower_better", "higher_better"),
  strict = TRUE
)
}
\arguments{
\item{selected_modules_file}{Step-6C \code{selected_modules.tsv}.}

\item{seq_smiles_manifest_file}{Step-7 manifest.}

\item{output_base}{New step-8 output directory.}

\item{binding_score_file}{Optional all-selected-module score table with
\code{module_uid}, target/protein, drug and score columns.}

\item{predictor}{Optional function called once per module with a data frame
containing \code{module_uid}, \code{target_name}, \code{drug_name}, \code{target_seq} and
\code{drug_smiles}. It must return one numeric score per row or a data frame
containing \code{binding_score}.}

\item{score_direction}{Whether lower or higher binding scores rank first.}

\item{strict}{Require one score for every selected DRN pair.}
}
\value{
Unified \code{binding_scores.tsv} with output paths in attributes.
}
\description{
Step 8 constructs drug-target pairs from each selected DRN and joins the
step-7 sequences/SMILES. Scores may be supplied in a tabular file (for
example DeepPurpose output) or calculated by a user function receiving the
module's DPI table. This keeps the selected-module boundary independent of
the Python environment.
}
