# Minimal example data

A non-identifiable, reduced example dataset will be added after the module
manifest contract is implemented. It will exercise every stage without
shipping patient-level study data.

