# Model artifacts

The trained core34 GradientBoosting artifact will be added only after the
training and inference code has been separated and regression-tested.

Every released model must include its feature schema, class order, software
versions, training-data checksum, validation metrics and a model card.

